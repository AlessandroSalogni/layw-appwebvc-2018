@model Doctor
@{
    ViewBag.PageHeader = "Patient: " + ((Patient)ViewBag.CurrentPatient).Name;
}

<div id="example">
    <div class="demo-section k-content">
        <div id="tabstrip">
            <ul>
                <li class="k-state-active">
                    Statistics
                </li>
                <li>
                    Diet
                </li>
                <li>
                    Training
                </li>
            </ul>
            <div>
                @Html.RenderPartialAsync("PatientStatistics");
            </div>
            <div>
                @Html.RenderPartialAsync("PatientDiet");
            </div>
            <div>
                @Html.RenderPartialAsync("PatientTraining");
            </div>
        </div>
    </div>
</div>

<script>
        $(document).ready(function () {
            $("#tabstrip").kendoTabStrip({
                animation: {
                    open: {
                        effects: "fadeIn"
                    }
                }
            });
        });
</script>


