@model string
@using Microsoft.AspNetCore.Authentication

@{ 
    Layout = "_LoginLayout";
}

<div style="text-align:center">
    @*@if (User?.Identity?.IsAuthenticated ?? false) { } else { }*@
    <h1>Welcome. Sign in Look After Your Weight</h1>
    <a class="btn btn-lg btn-info" href="/signin?returnUrl=%2F">Sign in</a>
</div>